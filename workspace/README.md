# Workspace

Small, self-contained experiments live here.

- [`spectrogram_word/`](spectrogram_word/) generates audio with a word embedded
  in its spectrogram and renders plots that reveal it.
- [`whatsapp/`](whatsapp/) removes timestamps and sender names from WhatsApp
  text exports while preserving message bodies.

Each project has its own setup and usage notes.
